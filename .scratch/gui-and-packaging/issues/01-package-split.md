# 01: 拆包重构——单文件拆成 aurumwatch 包，控制台行为不变

**What to build:** 把当前单文件监视脚本拆成 `aurumwatch` 包：判定核心（纯函数）、取数与解析、弹窗绘制、控制台编排、入口各归其位；`启动金价监视.bat` 改为从包入口启动。对用户而言行为零变化：双击后照旧是控制台行情上屏 + 越线弹窗提醒。本票是后续所有 GUI 改动的预备重构——先让后面每一刀都落在小文件上。

**Blocked by:** 无（可立即开工）

**Status:** ready-for-agent

- [ ] 从包入口（及改瘦后的 .bat）启动，行为与拆分前一致：两市场行情上屏、越线弹窗、故障不误报、长故障弹窗警告
- [ ] 判定核心保持纯函数：不碰网络、UI 与写屏
- [ ] 既有测试全部迁移到新模块路径并保持绿（判定、双市场、故障记账、解析与刷新对齐）
- [ ] 第三方依赖仍仅 requests；测试框架仍为标准库 unittest
- [ ] 数据源、单位、穿越触发、重新武装带、故障记账等语义零变更
- [ ] 弹窗行为不变：不抢焦点、点击关闭、到点自动消失
