.claude/settings.local.json
.vscode/
__pycache__/
*.pyc

# 本机生成的配置（用户数据，默认值在源码里）：换电脑时拷 exe 与它自己那份
config.json
config.json.bak
config.json.tmp

# 运行期生成的日志（见 ADR-0003）：不入库
logs/

# 发布产物（dist/）与构建中间物（build/）（见 ticket 07）：构建脚本每次现生成，不入库
dist/
build/
