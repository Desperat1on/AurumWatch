# Context — AurumWatch

领域术语表。只记录术语含义，不记录任何实现细节。

## 价格

**金价** — 单独使用时有歧义。本项目不单独使用「金价」一词，一律细分为「国内金价」或「国际金价」。

**国内金价** — 中国境内、以人民币/克计价的黄金价格。本项目取上海黄金交易所 Au99.99（行情代码「沪金99」）。

**国际金价** — 伦敦现货黄金（XAU/USD）价格，即「伦敦金」。以美元/盎司计价。

**行情数据时间** — 一条读数自带的行情时刻，不是程序取到它的时刻；用于判断数据新旧（休市时它静止不动）。
_避免_：更新时间、抓取时间

## 阈值与触发

**阈值** — 用户设定的、希望被提醒的价格水平。国内、国际两个市场，涨破、跌破两个方向，各一个阈值；可以只设其中一个。

**涨破** — 价格自阈值下方到达或越过阈值（≥）。

**跌破** — 价格自阈值上方到达或越过阈值（≤）。

**触发** — 某个阈值条件被满足、产生一次提醒的事件。

**穿越触发** — 触发规则：同一阈值触发后，须等价格回到阈值另一侧并越过「重新武装带」才重新具备触发资格，防止价格停留在阈值附近时反复提醒。

**重新武装带** — 价格回到阈值另一侧时需要超出的一小段缓冲（默认取阈值的 0.1%）。越过它才算「翻篇」，同一阈值方可再次触发。

## 故障

**数据源故障** — 某个市场本轮没能取到行情（网络不通、接口报错、响应解析不了）。是取数的问题，不是行情变化；出故障的轮次不参与阈值判定。

**故障警告** — 某市场连续数据源故障满一段时间（默认 10 分钟）时的一次性提醒，说明监视已失效。故障恢复后重新计时，再次长时间故障会再次警告。

## 监视状态

**市场状态** — 一个市场当前所处的情形，取值为「监视中」「已触发待回落」「数据源故障」三者之一。
_避免_：运行状态、健康状态

## 界面

**主窗口** — 常驻的行情面板：两个市场的现价、距阈值距离与市场状态，底部有事件记录与开关按钮。
_避免_：界面、控制台

**弹窗** — 触发或故障警告时出现在屏幕一角的小窗（默认右下角，位置可在设置里改为主屏四角之一），短暂停留后自动消失，不打断手头工作。
_避免_：通知、气泡

**设置** — 用户可调整的各项参数：阈值、提示音、外观、刷新等；不含只能改代码才能变的东西。
_避免_：选项、偏好、配置项

**预览** — 设置窗口内按当前（可能尚未保存的）设置渲染出的弹窗样例，改一项、变一处。
_避免_：示例、效果图

**事件记录** — 主窗口内按时间排列的近几次触发与故障警告，只活在本次运行的内存里。
_避免_：历史、消息

**日志** — 落盘保存的事件级记录，重启程序后仍可回查。
_避免_：与「事件记录」混用

## 发布

**发布产物** — 免安装的单文件程序：拷到任意 Windows 电脑双击即可运行，不需要预装 Python 或其它环境。
_避免_：安装包、绿色版、打包版
